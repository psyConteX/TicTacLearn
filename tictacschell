for i in board
    score=0
    if board[i] == current_player:
        score+=1
        if score>=3:
        return True
    else:
        score=0
    if (i+1) % 3 == 0:
        score=0
        #TODO
        #all the things
        